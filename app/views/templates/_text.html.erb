<div id="show_data" class="container">
  <%if @reel.script %>
  <%@reel.script.split('.').each_with_index do |text| %>
    <div class="card">
      <div class="card-body">
       <%= text %>
      </div>
    </div>
  <% end %>
  <% end %>
</div>