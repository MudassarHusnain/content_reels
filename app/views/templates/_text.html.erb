<div id="show_data" class="container">
  <% i = 0 %>
  <% text = @reel.script.split('.') %>
  <% text.each do %>
    <div class="card">
      <div class="card-body">
        <%= text[i] %>
        <% i = i + 1 %>
      </div>
    </div>
  <% end %>
</div>